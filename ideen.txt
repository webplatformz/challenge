Ideenskizze:

Abfolge:
1. Deck mischeln
2. Karten gleichmässig verteilen
3. Trumpf wird zufällig bestimmt
4. Spieler n+1 beginnt mit dem Ansagen (n ist letzter Spieler der angesagt hat)
5. Spiel beginnt
6. Spieler x spielt eine Karte (Referee überprüft Gültigkeit)
b) Spieler x+1..3 spielt eine Karte (Referee überprüft Gültigkeit)
7. Referee entscheidet wer gewinnt und übergibt Karten dem Gewinner
8. Gehe zu 6, Gewinner ist x
[Keine Karten mehr]
9. Ansage wird mit gemachten Punkte verglichen, Differenz dem Spieler gutgeschrieben
10. Hat ein Spieler mehr als 100 Punkte, Spielende, sonst zurück zu 1
 
Technisch
=================================
1. Stack.shuffle()
2.
Card card = Stack.drawCard()
player.assignCard(card) oder
List<Card> cards = Stack.drawCards()
player.assignCards(cards)
3.
CardType trump =  game.chooseTrump()
players.each(notify("TRUMP", trump))
4.
Number bid = player.getBid()
players.each(notify("BID", bid))
5.
players.each(notify("GAME_COMMENCES"))
6.
players.each {
 Card card = player.getCard()
 game.verify(playStack, card)
 playStack.add(card)
 players.each(notify("PLAY_STACK", playStack))
}
7.
Player player = game.determineVictor()
player.assignPlayedCards(playStack)
8.
while(game.moreCardsAvailable()) {
 // 6
} else {
9.
 players.each{
  Number difference = player.getDifference()
  player.addPoints(difference)
  notify("POINTS", player.getName(), player.getPoints())
 }
10.
 players.each{
  if(player.getPoints > 100) {
   Player player = game.calculateWinner()
   notify("WINNER", player.getName())
  } else {
   // 1
  }
 }
}
